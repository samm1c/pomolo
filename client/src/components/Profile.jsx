

function Profile({  }) {
    return (
        <div className='profile-window'>
            <button className="profile-row">Account</button>
            <button className="profile-row">Log out</button>
        </div>
    );
}

export default Profile;